import numpy as np
def randomization(n):
    """
    Arg:
      n - an integer
    Returns:
      A - a randomly-generated nx1 Numpy array.
    """
    list = [int(n), 1]
    output = np.random.random(list)
    return np.array(output)
    raise NotImplementedError

print(randomization(3))

#%%

#%%
